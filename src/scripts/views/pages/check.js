const Check = {
  async render() {
    return `
          <h2>Check, editing</h2>
        `;
  },

  async afterRender() {
    // Fungsi ini akan dipanggil setelah render()
  },
};

export default Check;
